import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#5B6ED9",
      dark: "#3451d1",
      light: "#eef2ff",
      contrastText: "#fff",
    },
  },
});

export default theme;